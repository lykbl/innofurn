extend type Mutation {
    addItem(sku: IntID! @rules(apply: ["exists:lunar_product_variants,sku"]), quantity: Int = 1 @rules(apply: ["min:1", "max:50"])): Cart!

    removeItem(sku: IntID! @rules(apply: ["exists:lunar_product_variants,sku"]), quantity: Int = 1 @rules(apply: ["min:1", "max:50"])): Cart!

    clearCartItem(sku: IntID! @rules(apply: ["exists:lunar_product_variants,sku"])): Cart!

    clearCart: Cart!
}
