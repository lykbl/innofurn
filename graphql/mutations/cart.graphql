extend type Mutation {
    addItem(productVariantId: IntID! @rules(apply: ["exists:lunar_product_variants,id"]), quantity: Int = 1 @rules(apply: ["min:1", "max:50"])): Cart!

    removeItem(productVariantId: IntID! @rules(apply: ["exists:lunar_product_variants,id"]), quantity: Int = 1 @rules(apply: ["min:1", "max:50"])): Cart!

    clearCartItem(productVariantId: IntID! @rules(apply: ["exists:lunar_product_variants,id"])): Cart!

    clearCart: Cart!
}
