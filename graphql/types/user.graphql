"Account of a person who utilizes this application."
type User {
    id: IntID!
    name: String!
    email: String!
    emailVerifiedAt: DateTime @rename(attribute: "email_verified_at")
    customer: Customer! @rename(attribute: "retailCustomer")
}
