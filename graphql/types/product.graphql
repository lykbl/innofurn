type Product implements WithUrls {
    id: ID!
    name(lang: Locales = ENGLISH): String
    description(lang: Locales = ENGLISH): String
    variants: [ProductVariant!]
    brand: Brand!
    collections: [Collection]

    urls: [Url]
    defaultUrl: Url
}
