type ChatRoom {
    id: IntID!
    user: User!
    messages: [ChatMessage!]!
}

union Author = Customer | Staff

type ChatMessage {
    id: IntID!
    body: String!
    chatRoom: ChatRoom!
    author: Author!
    createdAt: DateTime!
}
